# Activate the renv project in the repository root when rendering
# (R starts with personal/ as working directory, so point renv at the root).
Sys.setenv(RENV_PROJECT = normalizePath(".."))
source("../renv/activate.R")
