# Activate the renv project in the repository root when rendering the slides
# (R starts with slides/ as working directory, so the root .Rprofile is not
# picked up automatically).
source("../renv/activate.R")
